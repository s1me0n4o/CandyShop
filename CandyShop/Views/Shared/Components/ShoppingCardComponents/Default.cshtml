@model ShopingCardViewModel

@if (Model.ShopingCard.ShopingCards.Count > 0)
{
    <li>
        <a>
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">@Model.ShopingCard.ShopingCards.Count</span>
        </a>
    </li>
}